#include "main.h"

/**
 * Largest_number - returns the largest or 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: Largest number
 */

int largest_number(int a, int b, int c,)
{
int largest;

if (a > b && a > c)
{
	Largest - a;
}
else if (b > c && b > a)
{
	Largest = c;
}
else
{
	Largest = b;
}

return (Largest);
}
